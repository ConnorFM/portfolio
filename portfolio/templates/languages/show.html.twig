{% extends 'base.html.twig' %}

{% block title %}Languages{% endblock %}

{% block body %}
<div class="container admin">

<h1>Languages</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ language.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ language.Name }}</td>
            </tr>
            <tr>
                <th>Level</th>
                <td>{{ language.Level }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('languages_index') }}">back to list</a>

    <a href="{{ path('languages_edit', {'id': language.id}) }}">edit</a>

    {{ include('languages/_delete_form.html.twig') }}
</div>
{% endblock %}
