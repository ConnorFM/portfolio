<form method="post" action="{{ path('professional_experiences_delete', {'id': professional_experience.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ professional_experience.id) }}">
    <button class="btn">Delete</button>
</form>
